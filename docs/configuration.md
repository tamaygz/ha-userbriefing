# Configuration

User Briefing splits configuration across:

- `ConfigEntry.data` for profile identity and stable setup
- `ConfigEntry.options` for mutable profile preferences
- config subentries for per-snippet provider instances

See the spec files for the current contract details.